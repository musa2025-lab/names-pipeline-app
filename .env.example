# Copy to .env and fill in your own key. .env is gitignored - never commit it.
#
# Get a key from https://console.anthropic.com -> API keys.
# Use a key dedicated to this app so it can be revoked independently.
ANTHROPIC_API_KEY=sk-ant-...
